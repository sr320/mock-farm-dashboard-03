import LiveTemperaturePanel from '../components/LiveTemperaturePanel';

export default function LiveDataPage() {
  return (
    <main className="dashboard-main live-data-page">
      <LiveTemperaturePanel />
    </main>
  );
}
